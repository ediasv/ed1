#include "list.h"
#include <math.h>

void crivo(int n) {
  int i, sq = sqrt(n);
  List *l = create();
  /*Terminar!*/
  for (i = 2; i <= n; i++) {
    // oi
  }
  destroy(l);
}

int main() {
  int n = 30;
  crivo(n);
  return 0;
}
